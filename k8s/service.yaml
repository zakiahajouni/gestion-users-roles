apiVersion: v1
kind: Service
metadata:
  name: gestion-users-roles
  labels:
    app: gestion-users-roles
spec:
  type: ClusterIP
  selector:
    app: gestion-users-roles
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
